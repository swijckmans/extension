{
  "name": "@client-side-web/marketing-chrome-ext",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel watch src/manifest.json --host localhost --config @parcel/config-webextension --dist-dir dist",
    "build": "parcel build src/manifest.json --config @parcel/config-webextension --dist-dir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@client-side-web/typescript-config": "workspace:*",
    "@parcel/config-webextension": "^2.12.0",
    "@types/chrome": "^0.0.268",
    "parcel": "^2.12.0",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5.5.2"
  }
}
